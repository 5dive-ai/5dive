## Unreleased — feat(agent): manage Codex operating baseline (DIVE-3966)

- Codex agents now receive a versioned, marker-delimited operating baseline in `~/.codex/AGENTS.md` during provisioning and upgrades, while preserving user-authored instructions outside the managed block.
