## Unreleased — fix(ci): pin the 5dive-plugins clone to the last-green sha, unfreezing main (DIVE-4444)

A floating `git clone --depth 1` of 5dive-ai/5dive-plugins in the codex-channel harnesses let an
unrelated plugins merge (DIVE-3965) turn `test` and `test-installed-host` red on 5dive main with no
change to our tree, freezing every merge and release cut. All 10 clone sites (unit-tests.yml,
full-sweep.yml) now pin the plugins checkout to the last-green sha; the recovery-overwrite behaviour
the merge changed is reviewed on DIVE-4449, which bumps the pin forward once reconciled.
