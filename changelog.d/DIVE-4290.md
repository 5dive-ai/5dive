## Unreleased — feat(plugin): install GitHub repositories in one step (DIVE-4290)

### Added

- Add one-step plugin installation from GitHub marketplace repositories, including `plugins install` aliases and deterministic multi-plugin selection.
