## Unreleased

- fix(task): a human gate on a row with a bound PR now carries the tappable review link on every one of the three composers — the first ping, the `/inbox` batch re-send, and the unanswered-gate re-nag reminder (DIVE-4381)
