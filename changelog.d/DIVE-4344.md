## Unreleased — fix(task): default unset verification policy to delivered-only (DIVE-4344)

- Make boxes without an explicit verification policy defer grader sessions until a task is bound to a delivery.
