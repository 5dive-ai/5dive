## Unreleased — fix(grader-pool): a grade is in flight until the VERDICT, not until the row closes (DIVE-4322)

- Emit `task.graded` the moment `task verify` stores a verdict, and count a grade as in flight from its spawn to that verdict instead of to the row's close, so a PASS parked on an owed merge no longer holds a grader slot.
- Belt for rows graded before this ships: release a spawn whose row is done/cancelled, or whose verdict clock is strictly later than the spawn — pinned to the spawn so a stale `merge_owner` from an earlier pass cannot free a slot a grader is genuinely using.
