## Unreleased — fix(hermes): install without the browser tools inside the blocking exec (DIVE-4973)

- A cold `5dive agent install hermes` no longer waits for hermes' browser tools (agent-browser + Chromium). Upstream's 2026-09-24 installer made them part of the default install and pushed a cold install to 272-297s, just under the dashboard's 300s limit. The recipe passes `--skip-browser` when the installer supports it, then installs the browser tools in the background, so hermes agents still get them a few minutes later.
