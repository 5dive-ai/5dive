## Unreleased — fix(proof): the zero-human badge stops counting experiment-fixture rows as shipped work (DIVE-3227)

The published badge is `1 - asks/shipped`, and `shipped` counted 20 live-board
experiment-fixture rows (`stamp arm A..E`, filed by main/olivia/dev, all closed
2026-08-09) that carry no gate — pure denominator, so they could only flatter a
PUBLIC number. They are excluded at the METRIC now, by an anchored title prefix
defined once in `five_fixture_title_prefixes()` (`src/lib/tasks_db.sh`) and used
by the digest, `proof status` and the published corroborators alike; the rows are
untouched (they are a live experiment fixture).

The exclusion is never silent: `zero-human.json` gains `fixtureExclusion` (rule,
excluded-from-day, excluded-from-window), `history.jsonl` stamps the per-day
count, `5dive digest` prints it beside the KPI and `5dive proof status` names it
on the autonomy line. `proof publish` REFUSES (rc 4, nothing written) if the
digest it was handed cannot state its filter.
