# DIVE-4876 — the CHECKED text of the stored delivery result, as the recogniser reads it
# (DIVE-4828 pass 2's corpus). TREE lists the files at the delivered sha whose names
# the text mentions; CHANGED is the subset the delivered diff touched.
REPLAYABLE: yes
PACKAGE-TEST-SCRIPT: no
CHANGED-FILE: host-bin/5dive-storagebox-mirror.test.sh
---
CHECKED: bash host-bin/5dive-storagebox-mirror.test.sh → 91 passed, 0 failed (pristine main: 64/1, T19). M6 (no cred excludes) and M7 (no backups path exclude) both red as required. A manual --delete mutant on home_leg turns T25 red. T22 runs the real rsync over the local shim: 20 credential shapes planted, grep positive-controlled on the source (19+1), 0 on the receiver, all 7 work files arrive. Dry run over /home from agent-ops: 19.1G / 508k entries, 0 credential-shaped names.
CHECKED: bash host-bin/5dive-storagebox-mirror.test.sh → 118 passed, 0 failed at 4c83e07, both with a local borg 1.2.8 and with none installed (the suite unpacks borg from apt without root). M8 deletes each of the 28 credential patterns alone and every one leaks its planted file. T28 shows encryption at rest: the canary is in the archive and absent from the repo bytes, and the repo holds no key. T29 restores from the R2 key and fails on a foreign key. Init-script mutants go red: encryption=none 5, repokey 2, drill-on-local-key 2. Census: the shipped filters as rsync -n over /home + reaped + pg, from agent-ops, gave 469,369 entries and 0 hits for grep -cE '\.credentials\.json|/\.env[^/]*$|auth\.json'. The same grep on the unfiltered homes (positive control) gave 155.
CHECKED: bash host-bin/5dive-storagebox-mirror.test.sh → 123 passed, 0 failed at 282d2cd. M8: each of 36 patterns, deleted alone, leaks its plant. M10 and M11 red as required. Census (the shipped filters as rsync -n over /home + .5dive-reaped + the pg dir, from agent-ops; command on the body): 464,098 entries. quinn's wide grep → 264 filtered vs 1,770 unfiltered. Each of the 264 is classified on the body (18 .git refs + 246 source/doc files), and 0 remain once those are removed. The store-shaped grep → 0 filtered vs 264 unfiltered. comm -23 before and after: the new patterns remove only the 6 stores, .gemini/ tool state and 2 codex logs. The git-config regex over the real 119 configs drops 6.
