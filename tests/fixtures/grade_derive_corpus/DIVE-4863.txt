# DIVE-4863 — the CHECKED text of the stored delivery result, as the recogniser reads it
# (DIVE-4828 pass 2's corpus). TREE lists the files at the delivered sha whose names
# the text mentions; CHANGED is the subset the delivered diff touched.
REPLAYABLE: yes
PACKAGE-TEST-SCRIPT: no
CHANGED-FILE: install.sh
CHANGED-FILE: src/lib/models.sh
CHANGED-FILE: tests/per_model_effort_unit.sh
---
CHECKED: tests/per_model_effort_unit.sh 54 arms, 54 pass. 10 mutants (apply without the per-model key, heal overwriting, max not mapped, reader top-level-only, shaper top-level-only, shaper without alias resolution, heal without the no-op check, resolve_agent_effort legacy, heal touching codex, writer legacy) each red with 1-3 failing arms. byo_model_create_unit, pack_cross_harness_unit (83/83), agent_account_subverb_help_unit (36/36), team_import_type_override, pack_import_model_alias, agent_info_channels_binding, gate_card_model, agent_list_sudo, model_aliases, pack_agents_md, agent_sudo_fallback, human_at_pairing, codex_config_parity, agent_list_batch, quota_wall_health_surfaces, agent_coauthor_hook, codex_return_channel, gate_routed_reviewer_answer, install_monotonicity, self_update_restart_predicate, install_buzz_binaries, version_freeze_observer: all rc=0. agent_priv_read_breaker_unit is 5/4 on the branch AND on a clean origin/main control (already red, environment). The pre-push rail ran 6/6 changed harnesses green. Live run of the built bundle against fixture STATE_DIR/AGENT_HOME_ROOT: `agent _heal_effort` healed 2 of 2 seats with owner and mode 600 kept; `agent config y set effort=max` wrote top-level max and per-model xhigh; `agent info --json` reported xhigh. The 2.1.280 binary's modelSettings schema was read directly: enum low|medium|high|xhigh with .catch(undefined).
CHECKED: release-cut run 35813484310 succeeded; v0.49.1 published 03:51:41Z; git merge-base --is-ancestor f434fb28 v0.49.1 holds; tests/per_model_effort_unit.sh 54/54 at 7c77fe03 (verify check PASS on the row).
CHECKED: the installer's heal ran silently and exited 0: install.sh prints only a failure warn, and none appears in cli-install-catchup.log; every claude seat's settings.json was rewritten at 03:58:08. Pre-existing seat agent-ops: I removed its modelSettings, then `sudo 5dive agent _heal_effort` printed "healed on 1 agent(s)", exit 0. After the heal it carries effortLevel=high for all 4 families in models.sh (claude-opus-5-5, claude-sonnet-5, claude-fable-5-1, claude-haiku-4-5-20251001). A second pass healed 0. Fresh seat effortprobe on CC 2.1.280: the footer read "Opus 5.5 high" after create, "Opus 5.5 xhigh" after config set effort=xhigh, and "Opus 5.5 high" after config set effort=high. The seat was then removed and its home quarantined. Five pre-existing seats restarted by the self-update show footers matching their configured effort.
CHECKED: tests/per_model_effort_unit.sh 54/54 at 7c77fe03 (rc=0, re-run on deliver). 5dive --version on poke-two = 0.49.1. cli-install-catchup.log 03:58-04:02Z shows the stock self-update, with no heal warn between the codex-baseline and co-author lines. agent-quinn settings.json went at 03:58:08 from the 2 opus keys I recorded at iteration 1 to claude-opus-5-5, claude-sonnet-5, claude-fable-5-1 and claude-haiku-4-5-20251001 (each high, matching installed model_families/model_latest), and it kept claude-opus-5 (no overwrite). The main/creative/community settings.json mtimes are also 03:58:08. agent info quinn: effort high. Not re-derived by the verifier: the effortprobe and main pane-footer captures and the agent-ops strip-and-heal probe (both ops-recorded; this seat has no sudo or tmux access to other seats).
