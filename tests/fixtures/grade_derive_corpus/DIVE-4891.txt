# DIVE-4891 — the CHECKED text of the stored delivery result, as the recogniser reads it
# (DIVE-4828 pass 2's corpus). TREE lists the files at the delivered sha whose names
# the text mentions; CHANGED is the subset the delivered diff touched.
REPLAYABLE: yes
PACKAGE-TEST-SCRIPT: no
CHANGED-FILE: tests/council_mutants.sh
CHANGED-FILE: tests/run_all.sh
---
CHECKED: `bash tests/run_all.sh` (with FIVEDIVE_CORE_DIR = a built core at fcd81d73; the sealing harnesses run under sudo): 29/29 harnesses green, and every count equals the same harnesses run in core against core's built-in council on this host (engine 195, dispatch 110, cli_contract 66, constitution 41, veto_e2e 35, veto_window 35, roster_lineage 31, schedule 28, capture_e2e 27, registry_mode 27, cosign_unit 26, abstain 24, roster_class 21, capture_unit 20, liveness_unit 18, amend_e2e 17, amend_ballot 17, empty_round 15, ballot_tap 13, init_wizard 13, gate_e2e 12, ballot_e2e 8, bashroute 7, principal_redaction 7, cosign_e2e 6, liveness_e2e 6, notify_e2e 6, record_e2e 5, floor_diff 5). council_plugin_unit: 52/52 as root (P5 cross-writer lineage: core-sealed chain verifies in the plugin, the mixed chain verifies in both, and a tamper control goes red); 43 pass + 1 skip as a user. `bash tests/council_mutants.sh`: 5/5 red as required (seal back in-process, --json strip removed, rot-triage gate-file time lost, unmarked template edit, SQL reintroduced). Install probe with the real cmd_plugin_add against a file:// bare clone named 5dive-council: rc=0 ("'5dive council' now runs this plugin") on a core with council dropped from FIVEDIVE_BUILTIN_VERBS; rc=3 builtin-verb refusal on today's core, which is the correct control.
CHECKED: `bash tests/run_all.sh` (with FIVEDIVE_CORE_DIR = a built core; the sealing harnesses run under sudo): 29/29 carried harnesses plus council_plugin_unit green. It was re-run at 89a485a in a CI-like workspace (fresh clones of core main 853725f and of fcd81d73, the host's installed 5dive hidden from PATH) with 30/30 green, and every count equals the same harnesses run in core against core's built-in council on this host (engine 195, dispatch 110, cli_contract 66, constitution 41, veto_e2e 35, veto_window 35, roster_lineage 31, schedule 28, capture_e2e 27, registry_mode 27, cosign_unit 26, abstain 24, roster_class 21, capture_unit 20, liveness_unit 18, amend_e2e 17, amend_ballot 17, empty_round 15, ballot_tap 13, init_wizard 13, gate_e2e 12, ballot_e2e 8, bashroute 7, principal_redaction 7, cosign_e2e 6, liveness_e2e 6, notify_e2e 6, record_e2e 5, floor_diff 5). council_plugin_unit: 52/52 as root (P5 cross-writer lineage: core-sealed chain verifies in the plugin, the mixed chain verifies in both, and a tamper control goes red); 43 pass + 1 skip as a user. `bash tests/council_mutants.sh`: 5/5 red as required (seal back in-process, --json strip removed, rot-triage gate-file time lost, unmarked template edit, SQL reintroduced). Install probe with the real cmd_plugin_add against a file:// bare clone named 5dive-council: rc=0 ("'5dive council' now runs this plugin") on a core with council dropped from FIVEDIVE_BUILTIN_VERBS; rc=3 builtin-verb refusal on today's core, which is the correct control.
CHECKED: I reproduced CI's exact failure set first, without docker. The repro was `sudo unshare -m`: tmpfs over /var/lib/5dive, /etc/5dive, /opt/5dive, /usr/local/lib/5dive, /usr/local/share/5dive and /var/log/5dive, /dev/null bound over /usr/local/bin/5dive, an empty /etc/environment, and /etc/group minus `claude`. It ran as a user, from a fresh clone of the branch, against fresh builds of core main 853725f and fcd81d73. At 89a485a it gave FAIL for gate_e2e, notify_e2e, roster_lineage_e2e and veto_e2e ("cannot seal genesis"), SKIP for amend_e2e (agent registry) and SKIP for record_e2e (tasks store), the same six as CI. The same namespace with the group present passed. At 42c9d91 in the same namespace: without the group (control), run_all.sh refuses (rc=1, "box fixture missing"), and council_mutants.sh gives 0 red as required, 5 not (each refuses on a red baseline). With the group, run_all.sh passes 30/30 with no SKIP: gate_e2e 12, notify_e2e 6, roster_lineage 31, veto_e2e 35, amend_e2e 17, record_e2e 5, plugin_unit 52/52, and every other harness at the core counts already on the row. council_mutants.sh then gives 5 red as required, 0 not: M1 plugin_unit, M2 P6, M3 gate_e2e leg C, M4 and M5 plugin_unit.
